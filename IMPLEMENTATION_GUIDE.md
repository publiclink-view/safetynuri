# 안전누리 웹진 재구현 가이드

## 현재 상태

- 기존 Claude Code 구현본은 `temp/claude-round2/`에 보관되어 있다.
- 새 구현은 기존 코드를 수정하는 방식이 아니라 처음부터 다시 작성한다.
- 기존 구현본은 콘텐츠 문구와 추출 이미지 확인용으로만 참고한다.
- 기존 CSS를 그대로 복사하거나 새 구현의 출발점으로 사용하지 않는다.

## 디자인 기준

디자인의 최우선 기준은 다음 파일이다.

- `안전누리_웹진(시안용)_0616.pdf`
- `design_refs/page-1.png` ~ `design_refs/page-6.png`
- 메인 1:1 기준: `design_refs/m1-1.png` (`1920 × 6200`)
- 메인 섹션 참고: `design_refs/study_*.png`

코드 구조나 일반적인 웹 디자인 관례보다 PDF의 전체적인 인상, 구성, 비율, 정렬을 우선한다.

## 구현 순서

1. PDF에서 섹션 구조와 공통 정렬선을 분석한다.
2. HTML 마크업을 새로 작성한다.
3. 데스크톱 1920px 기준 레이아웃을 먼저 맞춘다.
4. 실제 브라우저 렌더를 기준 이미지와 비교한다.
5. 데스크톱 디자인이 안정된 후 반응형을 구현한다.
6. 페이지 하나를 완료하고 검수한 뒤 다음 페이지로 진행한다.

## CSS 파일 구조

기존 유지보수 방식을 가능한 한 유지한다.

```text
css/
├── fonts.css
├── base.css
├── common.css
├── main.css
└── sub.css
```

- `fonts.css`: 실제 사용하는 폰트와 굵기만 선언
- `base.css`: reset, 전역 변수, 기본 타이포그래피
- `common.css`: 헤더, 내비게이션, 푸터, 공통 요소
- `main.css`: `index.html` 전용
- `sub.css`: 상세 페이지 공통 및 페이지별 스타일

## CSS 작성 원칙

- 기존 방식처럼 읽기 쉬운 일반 클래스명을 사용한다.
- CSS 변수는 `:root`에 정리한다.
- Flex와 Grid를 중심으로 작성한다.
- 반복되는 박스는 가능한 한 `fr` 단위로 나눈다.
- 고정 픽셀값으로 각 카드의 너비를 일일이 지정하지 않는다.
- 과도한 선택자 중첩, 특이한 설계 체계, 유틸리티 클래스 남용을 피한다.
- `!important`와 복잡한 `calc()` 사용을 최소화한다.
- JavaScript에서 시각 스타일을 직접 지정하지 않는다.
- 미디어쿼리는 각 CSS 파일 하단에 모은다.
- 섹션별 주석을 넣어 빠르게 찾을 수 있게 한다.

## 박스와 비율

- 반복 콘텐츠 박스의 너비는 Grid의 `fr` 비율을 우선한다.
- 이미지와 영상 박스는 단순한 `aspect-ratio`를 사용한다.

권장 비율 예시:

```css
aspect-ratio: 1 / 1;
aspect-ratio: 4 / 3;
aspect-ratio: 3 / 2;
aspect-ratio: 16 / 9;
aspect-ratio: 3 / 4;
```

- `467 / 383`처럼 복잡한 비율은 사용하지 않는다.
- PDF와 몇 픽셀 차이가 나더라도 가장 가까운 단순 비율을 선택한다.
- 사진은 기본적으로 `object-fit: cover`를 사용하되 중요한 피사체가 잘리지 않도록 `object-position`을 조정한다.
- 고정 높이는 히어로처럼 디자인상 반드시 필요한 영역에만 사용한다.

## 이미지 처리

- `images/`에는 로고, 아이콘, 공통 배경처럼 기본 레이아웃과 디자인에 계속 사용하는 자산을 둔다.
- `contents/`에는 호별·시기별로 교체되는 기사, 카드뉴스, 썸네일 등 콘텐츠 성격의 이미지를 둔다.
- 공통 디자인 자산과 교체형 콘텐츠 이미지를 같은 폴더에 섞지 않는다.
- PDF에 포함된 이미지는 가능한 경우 개별 이미지로 추출한다.
- 개별 추출이 어렵다면 PDF에서 필요한 영역만 정확히 크롭한다.
- 사진과 그래픽 자산만 이미지로 사용한다.
- 제목, 본문, 버튼, 태그 등 HTML로 구현할 수 있는 UI와 텍스트를 통째로 이미지화하지 않는다.
- 추후 원본 이미지가 제공되면 같은 파일명으로 교체할 수 있게 의미 있는 파일명을 사용한다.
- 기존 추출 이미지는 `temp/claude-round2/contents/`에서 참고할 수 있다.

## 디자인 재현 기준

우선 확인할 항목:

1. 섹션 순서와 전체 높이 비율
2. 공통 콘텐츠의 좌우 정렬선
3. 히어로 배경 크롭과 초점 위치
4. 제목, 보조 문구, 워터마크의 위치와 비례
5. 카드의 크기, 비율, 간격과 겹침 관계
6. 섹션 배경색과 경계 위치
7. 헤더와 푸터의 전체 구성
8. 이미지 크롭과 피사체 위치

CSS 코드 취향보다 PDF 재현도를 우선하되, 미세한 픽셀 차이를 위해 유지보수성을 크게 해치지 않는다.

## 히어로 구현 유의사항

기존 구현에서 PDF와 가장 차이가 컸던 영역이다. 숫자를 조금씩 보정하지 말고 다음 기준점을 먼저 측정한다.

1. 헤더 하단
2. 제목 시작점
3. 보조 문구 시작점
4. 추천 카드 상단
5. 히어로와 파란 배경의 경계
6. 추천 콘텐츠 영역 하단

배경 이미지의 `object-fit`, `object-position`, 오버레이 농도까지 함께 비교한다.

## 반응형 원칙

- 데스크톱 1920px 기준 디자인을 먼저 완성한다.
- 태블릿과 모바일에서는 동일한 디자인 의도를 유지하되 자연스럽게 재배치한다.
- 유동 레이아웃의 실제 계산값이 0 또는 5로 끝나지 않아도 괜찮다.
- 모바일에서 가로 스크롤, 텍스트 잘림, 지나친 이미지 크롭이 생기지 않게 한다.
- `100vh`가 필요하면 모바일 브라우저를 고려해 `svh` 또는 `dvh` 보완을 검토한다.

## 보관본 사용 시 주의

- `temp/claude-round2/`는 참고용이며 새 페이지에서 직접 로드하지 않는다.
- 보관본 CSS 선택자와 새 CSS가 섞이지 않게 한다.
- 보관본에서 가져올 항목은 텍스트 콘텐츠와 사용 가능한 이미지로 제한한다.
- 새 구현이 안정될 때까지 보관 폴더를 삭제하지 않는다.

## 공통 영역과 서브페이지

- 메인 디자인이 확정된 뒤 헤더와 푸터를 `inc/header.html`, `inc/footer.html`로 분리한다.
- 헤더·푸터 스타일은 `css/common.css`에서 관리한다.
- 공통 JavaScript가 필요하면 `js/common.js`를 만들고 페이지 전용 동작과 분리한다.
- 서브페이지는 루트에 두며 공통 상세 스타일은 `css/sub.css`에 작성한다.
- 첫 서브페이지에서 공통 상세 레이아웃을 확정한 뒤 나머지 페이지에 재사용한다.
- 각 서브페이지는 데스크톱 구현과 검수를 마친 후 반응형 작업으로 넘어간다.
